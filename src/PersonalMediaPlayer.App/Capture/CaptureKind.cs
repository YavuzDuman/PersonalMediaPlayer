namespace PersonalMediaPlayer.App.Capture;

internal enum CaptureKind
{
    SelectedArea,
    SingleWindow,
    SingleMonitor,
    AllMonitors,
    Fullscreen,
    TextFile
}

internal enum CaptureOverlayKind
{
    Region,
    Monitor,
    Window
}
