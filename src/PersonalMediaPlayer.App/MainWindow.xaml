<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="PersonalMediaPlayer.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:PersonalMediaPlayer.App.Views"
    Title="Personal Media Player"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border x:Name="TitleBarHost" Height="48">
            <TitleBar
                x:Name="AppTitleBar"
                Title="Personal Media Player"
                BackRequested="TitleBar_BackRequested"
                IsBackButtonVisible="{x:Bind Shell.ContentFrame.CanGoBack, Mode=OneWay}"
                IsPaneToggleButtonVisible="True"
                PaneToggleRequested="TitleBar_PaneToggleRequested">
                <TitleBar.IconSource>
                    <ImageIconSource ImageSource="Assets/AppIcon.ico" />
                </TitleBar.IconSource>
            </TitleBar>
        </Border>

        <views:ShellPage x:Name="Shell" Grid.Row="1" />
    </Grid>
</Window>
